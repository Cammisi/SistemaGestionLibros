# Le decimos a Hibernate: "La base de datos ya está creada (por init.sql), solo valida que coincida"
spring.jpa.hibernate.ddl-auto=none
# Evitamos warnings de "Session closed" en los logs
spring.jpa.open-in-view=false

# Mejoramos el logging para ver si algo falla
logging.level.org.hibernate.SQL=DEBUG